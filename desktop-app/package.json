{
  "name": "desktop-app",
  "version": "1.4.2",
  "description": "ALS Automation Engine",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "clean": "node clean.js",
    "build": "npm run clean && node prebuild.js full && electron-builder --win --config.extraMetadata.edition=Full --config.artifactName=ALS-Full-Setup-1.4.2.exe --config.directories.output=C:\\Temp\\ALS-build && node postbuild.js",
    "build:full": "npm run clean && node prebuild.js full && electron-builder --win --config.extraMetadata.edition=Full --config.artifactName=ALS-Full-Setup-1.4.2.exe --config.directories.output=C:\\Temp\\ALS-build && node postbuild.js",
    "build:lite": "npm run clean && node prebuild.js lite && electron-builder --win --config.extraMetadata.edition=Lite --config.appId=com.als.dashboard.lite \"--config.productName=ALS Automation Engine Lite\" --config.artifactName=ALS-Lite-Setup-1.4.2.exe --config.directories.output=C:\\Temp\\ALS-build && node postbuild.js",
    "build:all": "npm run build:full && npm run build:lite",
    "test": "node --test tests/*.test.js"
  },
  "build": {
    "appId": "com.als.dashboard",
    "productName": "ALS Automation Engine",
    "artifactName": "ALS-${edition}-Setup-${version}.${ext}",
    "files": [
      "**/*",
      "bundled.env",
      "!tests/**",
      "!dist/**",
      "!releases/**",
      "!local_cache.json",
      "!local_cache.test-contaminated-backup.json",
      "!local_cache.test-*.json",
      "!implementation_plan.md",
      "!task.md"
    ],
    "win": {
      "target": "nsis",
      "icon": "icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "differentialPackage": false
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "dotenv": "^17.4.2",
    "node-cron": "^4.5.0",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "electron": "^42.5.0",
    "electron-builder": "^26.15.3"
  }
}
